import { ComingSoon } from "@/components/coming-soon";

export default function PropertiesPage() {
  return (
    <ComingSoon
      phase="Phase 2.5"
      title="Properties"
      description="Property list with service history per site. Lives alongside the jobs view as a second lens on the same data."
    />
  );
}
