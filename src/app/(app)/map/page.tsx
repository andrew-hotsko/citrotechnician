import { ComingSoon } from "@/components/coming-soon";

export default function MapPage() {
  return (
    <ComingSoon
      phase="Phase 4"
      title="Map"
      description="Google Maps view with region filters and pin clustering. Select pins to build a multi-stop trip for a tech — optimized by drive time."
    />
  );
}
