import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      phase="Phase 4"
      title="Settings"
      description="Team management, checklist templates per product, CSV import, and Salesforce sync controls."
    />
  );
}
